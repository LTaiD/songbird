# Render Blueprint (spec §4): backend as a Docker web service.
# Frontend deploys to Vercel separately; set FRONTEND_ORIGIN to its URL.
services:
  - type: web
    name: songbird-backend
    runtime: docker
    rootDir: backend
    plan: starter          # batch CV+audio wants more than free-tier RAM
    healthCheckPath: /health
    envVars:
      - key: FRONTEND_ORIGIN
        value: https://songbird.vercel.app   # <- replace with your Vercel URL
      - key: KERAS_BACKEND
        value: torch
