# Phase 0. opencv-contrib-python (not opencv-python) — it ships cv2.aruco.
opencv-contrib-python
mediapipe
numpy<2  # mediapipe/matplotlib/numba in common envs aren't numpy-2 ready yet

# Phase 1. Keras 3 on the torch backend (KERAS_BACKEND=torch); torch = MPS on mac.
keras
torch
