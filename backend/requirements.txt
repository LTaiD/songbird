# opencv-contrib-python (not opencv-python) — it ships cv2.aruco.
--extra-index-url https://download.pytorch.org/whl/cpu
opencv-contrib-python
mediapipe
numpy<2  # mediapipe/matplotlib/numba in common envs aren't numpy-2 ready yet

# Keras 3 on the torch backend (KERAS_BACKEND=torch); cpu wheels for Docker.
keras
torch

# audio (WHEN) + API
librosa
soundfile
fastapi
uvicorn[standard]
python-multipart
websockets
